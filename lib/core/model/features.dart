class FoodFeatures {
  String id;
  int number;

  int getNumber() => this.number;
  void setNumber(int num) {
    this.number = num;
  }

  void setId(String id) {
    this.id = id;
  }

  String getId() => this.id;
}
